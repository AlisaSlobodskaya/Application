plugins {
    id 'application'
}

group 'org.example'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    implementation group: 'org.javassist', name: 'javassist', version: '3.18.2-GA'
    testCompile group: 'junit', name: 'junit', version: '4.12'
}

run {
    dependsOn(jar)
    mainClassName = "D:\\Приложение2\\src\\main\\java\\jmx\\Main.java"
    jvmArgs("-javaagent:D:\\Приложение2\\build\\libs\\Приложение2-1.0-SNAPSHOT.jar")
}

jar{
    manifest{
        attributes(
                "Can-Redefine-Classes": true,
                "Can-Retransform-Classes": true,
                "Premain-Class" : "agent.Premain",
        )
    }
}
